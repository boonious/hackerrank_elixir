# https://www.hackerrank.com/challenges/lambda-calculus-reductions-1/problem
((λx.(x y))(λz.z)) = (x y)[x : = λz.z]
                   = (x [x : = λz.z] ) (y [x : = λz.z] )
                   = (λz.z) y
                   = y
